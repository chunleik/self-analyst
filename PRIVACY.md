# SelfAnalyst 隐私说明

SelfAnalyst 是本地优先的个人活动分析工具。当前版本不会采集屏幕截图、OCR 内容、麦克风声音、
系统回放声音或语音转写。

## 当前可能读取的数据

| 数据 | 读取目的 | 是否保存原文 |
|------|----------|--------------|
| 前台应用与系统窗口标题 | 构建活动时间线 | 保存标题 |
| UIA 控件树与文本 | 识别微信对话人、文章/文档/页面标题 | 否，仅在单次调用内使用 |
| 用户显式配置目录中的文件系统元数据 | 提供文件名、路径、大小和创建/修改时间查询 | 保存元数据；不读取或保存普通文件正文 |
| 用户与 Agent 的会话 | 提供会话和长期记忆功能 | 按会话/记忆配置保存在本机 |

## 内容事件边界

内容事件只保存：应用名、系统窗口标题、可选上下文标题、标题类型、来源、置信度、UIA 字符计数和
时间元数据。禁止保存 `text_content`、UIA 原文、控件树、正文、截图或可还原正文的节点列表。

UIA 查询前会排除密码管理器和认证类敏感应用。查询失败时只保留系统窗口标题，不使用截图或 OCR。

## 本地存储

| 数据 | 默认位置 |
|------|----------|
| 活动事件 | `{events.data-dir}/events.db` |
| 迁移备份 | 旧事件库及原始分区；仅用户显式确认后清理 |
| Wiki 与记忆派生数据 | 用户数据目录下的相应 SQLite/索引目录 |
| 桌面会话 | 用户数据目录下的 `chat.db` |
| 文件元数据 | `{memory.dir}/file-watch.db`，以及本地事件服务 `events.db` 中的文件元数据 heartbeat |

活动事件仅保存通过来源 schema 和隐私策略的字段，不保存 HTTP header、token 或完整请求。
内容只允许标题事实；文件事件只保存路径及文件系统元数据，不读取普通文件正文、内容哈希、摘要或 embedding。
连续等价心跳合并保存，无法逐条恢复每次采样。短期幂等回执保存身份、指纹和事件编号，不复制 payload。

合并存储适用于嵌入式服务，外部模式的数据目录不受 SelfAnalyst 控制。磁盘不足阻止新采集，不自动删除历史。
升级保留旧库备份；设置中明确确认清理后永久删除已登记备份，逐心跳恢复和旧格式回退能力随之失去。

升级不会自动删除旧版本已经产生的音频 bucket、OCR 样本目录或第三方工具目录，因为自动删除可能
造成不可恢复的数据损失。它们不会继续增长；用户确认备份后可自行清理。

旧文件语义索引和旧 `file-watch.db` 中的正文派生列会在通过安全校验后自动净化；这不适用于上述
OCR、音频或第三方工具历史目录。

## 联网边界

本地事件服务、标题识别和 accessibility sidecar 不需要外发 UIA 内容。启用 LLM、Embedding、
联网搜索或其他云服务时，会把对应功能所需的输入发送到用户配置的服务端；各功能必须遵守自己的
最小化规格。文件采集不读取或外发普通文件正文；FileTools 在本地匹配文件名、路径和时间等元数据，
但这些元数据作为 Agent 工具结果时可能进入用户配置的 LLM 会话。当前版本不存在云端 ASR 路径。

## 旧配置

旧 `config.toml` 中由[移除说明](docs/archive/removed-features/removed-ocr-audio.md)列出的 OCR 键和所有 `aw.audio.*`
会被识别并忽略，不会重新启用功能，也不会出现在桌面配置表单中。

## 建议

- 不需要联网能力时保持 Wiki LLM、Embedding 和联网搜索关闭。
- 只监控明确需要分析的文件目录。
- 定期检查本机数据目录和备份策略。
- 清理旧 OCR/声音数据前先做可恢复备份，并确认目标路径。

实现契约见 [上下文标题采集规格](openspec/specs/title-capture/spec.md) 与
[标题最小化持久化规格](openspec/specs/content-event-persistence/spec.md)，文件边界见
[文件元数据采集规范](openspec/specs/file-metadata-collection/spec.md)。
