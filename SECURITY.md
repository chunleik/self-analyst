# 安全策略 / Security Policy

感谢你帮助让 SelfAnalyst 更安全。本文档说明如何报告漏洞，以及当前的安全边界。

## 报告漏洞

**请不要通过公开 Issue 报告安全漏洞**——这会在修复前把攻击面暴露给所有人。

请通过 **GitHub Security Advisory** 私密报告：在本仓库 **Security → Report a vulnerability**
发起报告。该渠道仅维护者与报告者可见，可在修复完成后再公开。

报告时请尽量包含：受影响版本 / 提交、复现步骤、影响评估，以及（如有）建议的修复方向。

**响应预期**（尽力而为，非合同承诺）：

| 阶段 | 目标时间 |
|------|---------|
| 首次回复确认收到 | 7 天内 |
| 初步评估 / 是否受理 | 14 天内 |
| 修复并发布 | 视严重程度协商 |

修复发布后，如你愿意，我们会在致谢中署名报告者。

## 支持的版本

本项目处于早期阶段，仅对**最新 `main` 分支**提供安全修复。请基于最新代码复现与报告。

## 安全边界与设计说明

了解这些有助于判断什么算漏洞：

- **本地服务仅绑定回环地址**：内置 HTTP 服务监听 `127.0.0.1:5700`，不对局域网/公网开放，
  CORS 仅允许 `localhost` 来源。该服务**默认无身份认证**——其安全模型假设
  "本机当前用户可信"。同一台机器上的其它本地进程可访问该端口；在多用户或不可信的主机上
  运行时请知悉此风险。
- **API 密钥**：通过环境变量（`OPENAI_API_KEY` 等）或 `application.properties` 提供。
  **推荐用环境变量**，避免把密钥写进会被提交的文件。密钥仅用于调用你配置的服务，不会被
  SelfAnalyst 上传到其它地方。
- **采集到的数据默认只存本地**（见 [PRIVACY.md](PRIVACY.md)），不存在官方后端。
- **外发数据**：仅在相应功能开启时，文本会被发送到你**自行配置**的 LLM / Embedding /
  搜索服务端点。

## 我们尤其关注的漏洞类型

- 本地 `5700` 服务的越权访问、路径穿越、SSRF 或 CORS 绕过
- 配置 / 日志中 API 密钥或采集数据的意外泄露
- 文件监控 / OCR / 音频路径中的注入或越界读取
- 依赖项（Java / Rust / 捆绑二进制）中的已知高危漏洞

## 不属于漏洞的情形

- 在你**自己配置**的第三方 LLM 服务侧发生的数据留存——受该服务隐私政策约束，非本项目缺陷。
- 在不可信的多用户主机上，同机进程访问本地 `5700` 服务——这是上文已说明的设计边界，
  而非缺陷；如有更安全的加固方案欢迎以特性建议提出。

---

*本策略会随项目演进更新。*
