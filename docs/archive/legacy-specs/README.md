# Legacy 规格归档

本目录保存 OpenSpec 迁移前仍具有历史追溯价值的 SDD 文档，包括未逐项纳入主规格的实施决策、
测试条目和旧 `SPEC-*` ID。它们不是现行行为契约，也不再维护。

现行行为必须以仓库根目录 [`openspec/specs/`](../../../openspec/specs/) 下的主规格为准。已完整迁移且
没有独立历史价值的旧规格已从工作区删除，必要时可通过 Git 历史恢复。
