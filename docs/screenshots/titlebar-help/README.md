# 标题栏与帮助菜单布局验收

这些截图使用真实 desktop-ui 页面、示例时间轴内容及无头 Edge 渲染。`desktop-layout-*` 注入模拟桌面桥接，仅用于校验桌面布局，不是 Windows 原生窗口操作通过的证据；`web-*` 使用浏览器分支。

覆盖中文、英文，800×600、1200×800，文件入口开启，菜单展开及 Web 关于对话框。没有使用生产活动或会话数据。窗口拖动、贴靠、托盘及多显示器验收状态见 OpenSpec change `add-desktop-titlebar-help` 的 verification.md。
