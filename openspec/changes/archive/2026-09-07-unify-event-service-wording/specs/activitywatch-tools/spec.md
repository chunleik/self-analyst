## REMOVED Requirements

### Requirement: SPEC-AW-001 base URL 与路径
**Reason**: capability 重命名为 `event-query-tools`，与实现类型 `EventQueryTools` 对齐；行为契约不变。
**Migration**: 见 `openspec/specs/event-query-tools/spec.md` 中同 ID 的 requirement，内容原样承接。

### Requirement: SPEC-AW-002 工具方法
**Reason**: capability 重命名为 `event-query-tools`；行为契约不变，仅 endpoint 描述改用兼容 API 用语。
**Migration**: 见 `openspec/specs/event-query-tools/spec.md` 中同 ID 的 requirement。

### Requirement: SPEC-AW-003 事件查询参数
**Reason**: capability 重命名为 `event-query-tools`；行为契约不变。
**Migration**: 见 `openspec/specs/event-query-tools/spec.md` 中同 ID 的 requirement，内容原样承接。

### Requirement: SPEC-AW-004 AQL 请求
**Reason**: capability 重命名为 `event-query-tools`；行为契约不变。
**Migration**: 见 `openspec/specs/event-query-tools/spec.md` 中同 ID 的 requirement，内容原样承接。

### Requirement: SPEC-AW-005 错误降级
**Reason**: capability 重命名为 `event-query-tools`；行为契约不变，仅场景名与描述改用事件服务用语。
**Migration**: 见 `openspec/specs/event-query-tools/spec.md` 中同 ID 的 requirement。

### Requirement: SPEC-AW-006 超时
**Reason**: capability 重命名为 `event-query-tools`；行为契约不变，仅场景描述改用事件服务用语。
**Migration**: 见 `openspec/specs/event-query-tools/spec.md` 中同 ID 的 requirement。

### Requirement: SPEC-AW-007 Agent 工具可发现性
**Reason**: capability 重命名为 `event-query-tools`；行为契约不变，品牌名禁令条款原样保留。
**Migration**: 见 `openspec/specs/event-query-tools/spec.md` 中同 ID 的 requirement，内容原样承接。
