## 1. 路径修复

- [x] 1.1 在桌面壳添加 Java 路径边界回归测试，覆盖扩展磁盘路径、UNC、中文空格及普通路径，先验证旧行为失败。
- [x] 1.2 实现并接入无损路径转换，运行桌面壳 cargo test，确认原有生命周期测试和新增测试通过。

## 2. 分发验证

- [x] 2.1 增加安装布局桌面启动冒烟脚本，使用独立临时数据并禁用采集，验证真实桌面壳后端健康握手；接入安装检查。
- [x] 2.2 使用修复桌面壳与已验证的 v0.2.3 后端/runtime 构建 NSIS 安装包，验证解包后资源和真实启动链路，输出安装包及 SHA-256。

## 3. 规格与归档

- [x] 3.1 同步 desktop-shell 主规格并记录验证结果，运行 OpenSpec 严格校验后归档 change。



