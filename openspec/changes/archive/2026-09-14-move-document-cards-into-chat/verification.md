# 验证记录

- 完整桌面 UI 套件：`node --test self-analyst-app/src/test/js/*.test.mjs`，121 项通过。
- 回归覆盖：服务端标识去重、跨会话旧响应、分页后刷新、加载失败重试、多轮多文件归属、处理中与回答失败、保存取消/失败/重试、浏览器下载路径、保存反馈重挂载、流式附件跟随和正文不变。
- 使用实际 index.html、styles.css、Deep Chat bundle、消息适配器及文档渲染代码，在本地浏览器预览中注入两轮测试消息和服务端形状的文档元数据。
- 1280×720 与 800×720：增强消息面和 fallback 均显示对应轮次文件；窄窗口下长中文文件名正常显示，文件按钮可滚动到达。右侧无文档列表，会话内保留折叠入口。
- 浏览器点击下载显示“已发起下载”。模拟 Tauri 调用成功显示“已保存”；Node 测试覆盖取消和失败。未重测真实系统保存对话框，原生保存命令及后端内容接口未修改。
- 本次为前端资源变更，未运行 Java/Rust 全量测试，未打包或替换已安装桌面程序。
- 用户说明保留并更新 `docs/document-generation.md`；历史发布说明及 `docs/archive/` 保持原状。
