## 1. 共享统计基础

- [x] 1.1 在 self-analyst-events 增加共享04:00统计日历及对应测试；验证03:59/04:00、周一/月首、闰日、夏令时、半日和滚动十四天边界。
- [x] 1.2 在 EventStore 增加只读快照下的相交事件分页或流式入口，保留 queryEvents 语义；以 EventStore 区间测试验证跨起点长事件、end排他、小数秒、零时长及超过50000条无遗漏，记录查询计划和内存表现。
- [x] 1.3 在 self-analyst-events 增加区间聚合器与覆盖模型；通过测试验证完整/部分AFK、AFK并集、未知状态、空缺覆盖、重复窗口及跨应用冲突处理，精确总计等于已知应用加未知活动。

## 2. 桌面摘要与行为事实

- [x] 2.1 改造 SummaryService 耗时与行为比较使用共享统计器；扩充 SummaryServiceTest，验证真实缺陷的匿名夹具、未知标题、未知应用、AFK缺失与查询失败，并验证晚间22:00–04:00归属和扣除。
- [x] 2.2 改造 SummaryWindowClassifier、旧 getTimeline 路径及 SummaryTimelineAssembler 使用统一04:00边界；通过测试验证凌晨上午条目、跨周/月、滚动十四天部分子段无多算和已结束段不现场调用LLM。
- [x] 2.3 扩展 LocalFacts、摘要响应及提示输入的未识别活动/覆盖信息，在 desktop-ui 详情和中英文文案中展示；通过 Java/Node 测试及界面截图验证已知应用无 unknown 后缀，未知、仅AFK、无数据、失败文案可区分，原顶层API兼容。
- [x] 2.4 为 SummarySnapshot、SummarySnapshotStore、事实指纹、控制器直返路径和前端缓存加入版本/时区兼容判断；扩充快照及组装测试，验证旧文案与建议失效、首次新事实生成、兼容快照即时展示和损坏降级。

## 3. Wiki 一致性与历史迁移

- [x] 3.1 调整 self-analyst-wiki 的周期计算、发现、父子依赖和时间查询以使用04:00日历；通过周期及worker测试验证后半天跨午夜、月首凌晨、ISO双周配对、HOUR不变和未闭合周期不生成。
- [x] 3.2 改造 WikiFactBuilder、metrics及摘要提示使用共享统计结果并提升事实版本；扩充 WikiFactBuilderTest，验证超过2000条无截断、AFK覆盖说明、未知活动、父级聚合及同快照下与桌面指标一致。
- [x] 3.3 为 WikiStore/schema 和条目身份引入统计/日历版本，保留旧结果并提供同版本唯一性；通过临时旧数据库夹具验证事务迁移、重复启动幂等、同边界不同版本HOUR共存、失败保留旧数据和备份恢复。
- [x] 3.4 在Wiki默认查询、桌面读取、父依赖及语义检索中隔离旧版本；通过测试验证旧日/周摘要和向量结果不能回流新时间轴，新版本未完成时桌面按本地事实降级。
- [x] 3.5 将历史新版本重算纳入受限后台发现/重试流程；通过worker测试验证依赖顺序、预算限制、LLM不可用、重启续算、重复发现以及启动/看板不阻塞，确认原始事件未被改写。

## 4. 集成验收与文档

- [x] 4.1 先运行上述对应模块的针对性测试，再运行 `mvn test`；记录所有结果，跨模块验证共享口径、迁移恢复与永久原始事件不变。
- [x] 4.2 使用匿名跨04:00夹具及本机只读事件重算核对修复前后结果；记录区间边界、AFK交集、已知应用和未知活动，不把此前午夜口径的996.811秒当作新口径期望值，也不提交私有数据。
- [x] 4.3 同步 README.md 英文、README.zh-CN.md 中文及相关用户说明，说明04:00统计日、未知活动和旧摘要重算；检查完整README差异并提供中英文详情截图作为验收证据。
- [x] 4.4 使用 openspec-sync-specs 同步本change涉及的四项主规格，运行 `openspec validate fix-activity-statistics-day-boundary --strict` 及主规格严格验证；确认任务完成和测试通过后使用 openspec-archive-change 归档，并保留验证记录。

实施交付遵循功能分支和PR流程；提交、推送及合并按用户授权执行，合并前确认最新提交的必需检查（包括Windows全量验证）成功。
