## 1. 合并写入与事务

- [x] 1.1 在 `self-analyst-events/src/test/java` 增加固定时钟合并回归用例，覆盖相同 data 不同键序、边界、A/B/A、累计 duration、短 duration、乱序及非法时间；先确认旧实现不能满足新增契约。
- [x] 1.2 在 `self-analyst-events` 的 store/ingestion 中实现直接事务性合并与当前连续活动管理，验证上述用例及同 bucket 并发提交无丢失更新。
- [x] 1.3 在事件库实现 24 小时持久回执、冲突校验与过期维护；验证响应丢失后重试、重启重试、已删除目标不复活、有效期不被重试延长及磁盘失败回滚。
- [x] 1.4 改造 events、批量 events 和 import 写入以脱离 raw，保留独立事件并提供有界重试语义；通过独立相同数据、部分非法批次、重复导入及事务失败测试。
- [x] 1.5 调整 `EventServer` 启动装配，新格式不创建 raw 分区或来源/coverage/checkpoint 表；通过空目录启动和持续写入检查，并运行 `mvn -pl self-analyst-events -am test`。

## 2. 采集连续性与隐私

- [x] 2.1 在 window/AFK watcher 中引入采集会话和中断后累计时长重置，验证启动、休眠、失败恢复、时钟回拨及重试身份复用用例。
- [x] 2.2 在 `self-analyst-content` 及文件采集客户端对齐会话/重试协议和过期重试限制，验证采样周期保持、标题切换及时、文件节流不变及外部 ActivityWatch 模式兼容。
- [x] 2.3 更新内容与文件持久化测试，验证 heartbeat/events/import 全入口拒绝正文及未知字段，事件库、回执、日志均无秘密标记；运行相关模块针对性 Maven 测试。

## 3. 格式迁移与恢复

- [x] 3.1 在应用存储格式识别及 events 存储初始化中定义新格式和迁移阶段记录，验证空目录、现行旧格式、未知/较新格式及旧开发期 aw.db 隔离用例。
- [x] 3.2 实现独占锁、空间预检和包含 WAL 已提交数据的一致工作副本，验证锁冲突、磁盘不足及源文件未修改。
- [x] 3.3 实现旧投影基线与待处理 raw 核对，只补齐可判定提交，验证已删除事件/bucket 不复活、raw 缺失/损坏/歧义时阻止切换。
- [x] 3.4 构建不含永久 raw 关联的紧凑目标并校验 ID、data、时间、时长、bucket 摘要及有效回执，验证迁移前后普通查询结果一致。
- [x] 3.5 实现校验目标后先发布根版本 2 再切换活动文件、阶段恢复和旧库备份登记，在每个迁移边界注入中断验证可恢复、不混合写入及新数据存在后不自动回退旧快照。
- [x] 3.6 验证旧程序对新格式的拒绝行为并记录降级限制；通过独立 fixture 测试及可复核恢复说明，不在用户真实目录运行迁移实验。

## 4. 接口、导出与容量界面

- [x] 4.1 在原始查询/重建接口实现认证后的 `410/RAW_STORAGE_RETIRED`，移除在线 raw/recovery 装配；通过缺少凭据、有效凭据、备份存在和外部模式测试。
- [x] 4.2 在 `self-analyst-app` 文档导出服务中退役原始来源、兼容普通事件来源并实现一致快照分页，验证活动事件并发延长时无重复遗漏、原始来源不被静默替换及已有成果可下载。
- [x] 4.3 调整 raw/projector 配置处理与桌面设置提示，验证旧配置提示退役但新格式可启动、不会重新创建 raw，并检查迁移仍能读取旧目录配置。
- [x] 4.4 调整桌面存储状态 API 与 `desktop-ui`，分别显示活动库、辅助文件、备份、临时文件及迁移/健康状态；通过 `src/test/js` 对应 Node 测试并留存 UI 截图。
- [x] 4.5 实现已登记迁移备份的显式清理预览与确认，验证拒绝活动库重叠、越界、重解析点及未登记文件，覆盖外置 rawRoot 和清理中断重试。
- [x] 4.6 验证新库损坏、磁盘不足时明确阻断且不自动建空库/恢复过时 raw/删除历史；检查桌面错误与日志不泄露事件内容。

## 5. 综合验证与文档

- [x] 5.1 增加十万条相同心跳和多日固定时钟容量测试，验证仅一个永久活动区间、回执过期回收及稳态大小；按 design.md 与旧双层 fixture 对比，记录新库不超过旧基线 20% 的限定样本验收及各组成大小。
- [x] 5.2 运行跨模块 `mvn test`，覆盖查询、AQL、Wiki、统计、文件根过滤、导入导出和迁移集成；所有失败修复后保留命令与结果。
- [x] 5.3 更新 `README.md`、`README.zh-CN.md`、`docs/architecture.md` 和配置/维护文档，说明权威合并记录、原始能力退役、24 小时去重、备份清理及降级限制；检查完整 README 差异确保双语一致。
- [x] 5.4 使用 openspec-sync-specs 同步八项能力增量，删除已退役 raw 主规格（含 Purpose），通过历史 change 保留 ID 追溯；运行 `openspec validate adopt-merged-event-storage --strict` 及全部主规格严格校验，确认无继续承诺永久逐心跳能力的冲突条款。
- [x] 5.5 全部任务与验证完成后使用 openspec-archive-change 归档，记录迁移/容量证据和保留文档；通过归档结果确认主规格已同步。



## 6. PR 跨平台回归

- [x] 6.1 修正 RuntimeStorageGuardTest 的临时路径夹具，使用准入后的真实路径，并确认断电注入确实发生于提交钩子；运行数据守卫针对性测试；最新 PR 的跨平台守卫及 Windows 全量验证由交付流程单独跟踪，不以本地结果替代。



