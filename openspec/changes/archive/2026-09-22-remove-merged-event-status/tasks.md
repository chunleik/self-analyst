## 1. 顶部状态栏

- [x] 1.1 移除 index.html、init.js、ui.js 中合并事件状态项及两种语言的专用标签，检查不存在残留绑定。
- [x] 1.2 调整已有状态与摘要测试夹具，在不提供合并事件 DOM 的情况下验证状态刷新；运行桌面 Node 测试。

## 2. 规格收尾

- [x] 2.1 同步 merged-event-storage 主规格，运行 OpenSpec 严格校验并归档变更。

验证：桌面 Node 测试 166 项通过；change 及 28 项主规格严格校验通过；git diff --check 通过。

文档：docs/ 下历史设计、发布说明及合并存储验证记录保持历史语义，无需调整；本次显示契约已同步主规格。design.md 按条件省略，此变更无架构决策。
