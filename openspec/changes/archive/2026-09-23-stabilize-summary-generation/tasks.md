## 1. 引用派生与误拒（先完成）

- [x] 1.1 修改 WikiEvidencePolicy/WikiSummarizer 与当前窗提示词，apps/evidence本地派生；安装器/主程序/邮件跨应用、未知ID及旧JSON回归通过。
- [x] 1.2 修复带动作字样的标题元描述误拒，真实失败脱敏句与独立肯定/否定反例通过；Wiki与当前窗定向验证后再进入阶段2；三日复验补充界面名称枚举误拒回归。

阶段1验证：旧实现44项Wiki定向中5失败8错误；修复后Wiki44项、当前窗12项及Node167项通过。日志 .stability-phase1-red.log / .stability-phase1-green.log 为忽略的本地运行产物。没有调用真实模型。

## 2. 周期预算与持久化检查点

- [x] 2.1 实现独立WikiGenerationStore、原子预留/幂等结算/未知请求保守恢复；测试并发、跨重启/日期/模型/输入不重置周期额度。
- [x] 2.2 接入Pipeline所有阶段与FINAL检查点、稳定模型身份及正式plain详细usage，验证失败只重试缺失节点、发布失败零模型恢复、无输出上限参数；关闭SDK隐式重试并验证HTTP尝试数。
- [x] 2.3 接入配置/运行数据准入/关闭顺序/worker失败分类与暂停恢复，确保暂停不饿死其他任务；查询和桌面展示暂停原因及消耗，定向测试通过。

阶段2验证：Wiki65项、应用73项定向及Node169项通过，含真实本地发布失败的FINAL恢复、配置revision唤醒、额度调高且账本不清零、暂停不饿死其它周期、含预留的计量展示。日志 .stability-phase2.log 为本地产物。

## 3. 主题聚合与覆盖

- [x] 3.1 实现全局标题候选规划、主题卡片与成员账本、分阶段合并，测试跨应用主题、同层短任务及根预算不连带丢卡片。
- [x] 3.2 新增500/750/1000事实脱敏回归，验证计划确定性、可行全输入与不足预算覆盖、引用/统计不变；报告结构归属与语义代理区别。

阶段3验证：94项定向Java与169项Node通过；随后完整Maven包含月级210事实/大子摘要及DIRECT边界新增回归。500/750/1000固定集16项验收通过，初次暴露的时间层偏选已修复，未弱化断言。

## 4. 效果验证与收尾

- [x] 4.1 统一评测与正式plain调用配置，完成合成及必要已授权三日复验，记录真实成本、重试、持久恢复和人工语义审阅局限。
- [x] 4.2 运行匹配的定向和完整Maven/Node测试，审查差异、双语README、架构/用户说明；同步主规格、严格校验并归档。

最终验收：完整离线Maven 944项Java（6项既有跳过）、Node169通过；最后类型化错误定向24项Java通过。三日均生成成功、没有预算裁剪输入或卡片，三次进程恢复均0新增调用且内容/账本一致；未归类及真实语义不足详见docs/summary-quality-results.md。实际复验使用300秒整树上限，产品worker为180秒，报告已明确差异。29项主规格同步并严格验证通过，差异检查通过。

保留docs/architecture.md作为现行架构说明，docs/summary-quality-evaluation.md作为可重复使用的评测指南，docs/summary-quality-results.md作为分阶段验证历史；README双语同步。无用户配置、数据库或日志进入版本控制产物。
