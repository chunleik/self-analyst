<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.selfanalyst</groupId>
        <artifactId>self-analyst</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>self-analyst-app</artifactId>
    <packaging>jar</packaging>

    <name>SelfAnalyst :: App</name>
    <description>SelfAnalyst app — embedded AW server, LLM agent, desktop UI API</description>

    <dependencies>
        <dependency>
            <groupId>com.selfanalyst</groupId>
            <artifactId>self-analyst-events</artifactId>
        </dependency>
        <dependency>
            <groupId>com.selfanalyst</groupId>
            <artifactId>self-analyst-content</artifactId>
        </dependency>
        <dependency>
            <groupId>com.selfanalyst</groupId>
            <artifactId>self-analyst-wiki</artifactId>
        </dependency>
        <dependency>
            <groupId>com.selfanalyst</groupId>
            <artifactId>self-analyst-file</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-harness</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-model-openai</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <!-- TOML v1.0 parser (position-aware errors). SPEC-TOML-DEC-007 -->
            <groupId>org.tomlj</groupId>
            <artifactId>tomlj</artifactId>
            <version>1.1.1</version>
        </dependency>
        <dependency>
            <!-- tiktoken (cl100k_base) Java port for accurate local token counting -->
            <groupId>com.knuddels</groupId>
            <artifactId>jtokkit</artifactId>
            <version>1.1.0</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>desktop-ui-js-tests</id>
                        <phase>test</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${skipTests}</skip>
                            <executable>node</executable>
                            <workingDirectory>${project.basedir}</workingDirectory>
                            <arguments>
                                <argument>--test</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.selfanalyst.App</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                    </execution>
                </executions>
                <configuration>
                    <filters>
                        <!-- mcp-core 0.17.0 duplicates these byte-identical API classes from
                             mcp-json. Keep the dedicated mcp-json copy in the executable JAR. -->
                        <filter>
                            <artifact>io.modelcontextprotocol.sdk:mcp-core</artifact>
                            <excludes>
                                <exclude>io/modelcontextprotocol/json/**</exclude>
                            </excludes>
                        </filter>
                        <!-- jna and jna-platform carry identical license aliases. -->
                        <filter>
                            <artifact>net.java.dev.jna:jna-platform</artifact>
                            <excludes>
                                <exclude>META-INF/AL2.0</exclude>
                                <exclude>META-INF/LGPL2.1</exclude>
                            </excludes>
                        </filter>
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/*.SF</exclude>
                                <exclude>META-INF/*.DSA</exclude>
                                <exclude>META-INF/*.RSA</exclude>
                                <exclude>META-INF/MANIFEST.MF</exclude>
                                <exclude>module-info.class</exclude>
                                <exclude>META-INF/versions/**/module-info.class</exclude>
                                <exclude>META-INF/versions/**/OSGI-INF/MANIFEST.MF</exclude>
                                <exclude>META-INF/kotlin-project-structure-metadata.json</exclude>
                            </excludes>
                        </filter>
                    </filters>
                    <transformers>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheLicenseResourceTransformer"/>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheNoticeResourceTransformer"/>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                            <mainClass>com.selfanalyst.App</mainClass>
                        </transformer>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                    </transformers>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
